# Ласкаво просимо до Xynapse IDE

Xynapse IDE — це iнтегроване середовище розробки з вбудованим AI-асистентом.

## Можливостi

- **Чат з AI** — задавайте питання про код, отримуйте пояснення та генеруйте рiшення
- **Автодоповнення** — розумнi пiдказки прямо в редакторi (Tab для прийняття)
- **Inline-редагування** — видiлiть код i натиснiть `Ctrl+I` для AI-редагування
- **Контекст проекту** — асистент бачить ваш код, diff, термiнал та помилки
- **Council** — мультиагентне обговорення архiтектурних рiшень (`/council`)
- **Росiйськi моделi** — пiдтримка YandexGPT та GigaChat з коробки

## Вашi данi в безпецi

Xynapse не надсилає данi на сервери IDE. Всi API-ключi зберiгаються **локально** в `~/.xynapse/config.yaml`. Телеметрiя повнiстю вимкнена.

## Початок роботи

1. Вiдкрийте бiчну панель **Xynapse** (iконка злiва)
2. Налаштуйте моделi в `~/.xynapse/config.yaml`
3. Використовуйте `Ctrl+L` для чату, `Ctrl+I` для редагування, `Tab` для автодоповнення
